import React, { memo } from 'react'
import { AddProfileProps } from '../../interface';

const AddProfile:React.FC<AddProfileProps> = memo(({ onSubmit }) => {
  return (
    <div>AddProfile</div>
  )
})

export default AddProfile;