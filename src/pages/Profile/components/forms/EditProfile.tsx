import React, { memo } from 'react'
import { EditProfileProps } from '../../interface';

const EditProfile:React.FC<EditProfileProps> = memo(({ onSubmit }) => {
  return (
    <div>EditProfile</div>
  )
})

export default EditProfile